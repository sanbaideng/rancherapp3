replicaCount: 1

image:
  repository: reg.cecii.cn/pub/evolution
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8081
  nodePort: 30004
  targetPort: 8080

ingress:
  enabled: true
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  path: /
  hosts: ${host}
  tls: []
resources: {}
nodeSelector: {}

tolerations: []

affinity: {}
