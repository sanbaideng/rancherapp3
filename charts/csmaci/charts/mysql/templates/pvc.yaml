apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pvc }}
  namespace: {{ .Values.global.namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: {{ .Values.capacity }}
  selector:
    matchLabels:
      name: {{ .Values.pv }}
