apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.appconfig.pv | quote }}
  labels:
    name: {{ .Values.appconfig.pv | quote }}
    namespace: {{ .Values.global.namespace | quote }}
spec:
  capacity:
    storage: {{ .Values.appconfig.capacity }}
  accessModes:
    - ReadWriteOnce
  cephfs:
    monitors:
      - {{ .Values.global.storageAddr }}
    path: {{ .Values.appconfig.path }}
    user: {{ .Values.global.cephUser }}
    secretRef:
      name: {{ .Values.global.cephSecret }}
      namespace: default
    readOnly: false
  persistentVolumeReclaimPolicy: Retain
