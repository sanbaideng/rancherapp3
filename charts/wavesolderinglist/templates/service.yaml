apiVersion: v1
kind: Service
metadata:
  name: { { template "wavesolderinglist.fullname" . } }
  labels:
    app: { { template "wavesolderinglist.name" . } }
    chart: { { template "wavesolderinglist.chart" . } }
    release: { { .Release.Name } }
    heritage: { { .Release.Service } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
      protocol: TCP
      name: http
  selector:
    app: { { template "wavesolderinglist.name" . } }
    release: { { .Release.Name } }
