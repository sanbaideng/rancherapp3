apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.appmain.pvc }}
  namespace: 1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: {{ .Values.appmain.capacity }}
  selector:
    matchLabels:
      name: {{ .Values.appmain.pv }}