apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.mysql.pv }}
  labels:
    name: {{ .Values.mysql.pv }}
    namespace: {{ .Values.global.namespace | quote }}
spec:
  capacity:
    storage: {{ .Values.mysql.capacity }}
  accessModes:
    - ReadWriteOnce
  cephfs:
    monitors:
      - {{ .Values.global.storageAddr }}
    path: {{ .Values.mysql.path }}
    user: {{ .Values.global.cephUser }}
    secretRef:
      name: {{ .Values.global.cephSecret }}
      namespace: default
    readOnly: false
  persistentVolumeReclaimPolicy: Retain