apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "gitlab.fullname" . }}
spec:
  selector:
    matchLabels:
      app: {{ template "gitlab.fullname" . }}
  strategy: 
    type: Recreate
  template: 
    metadata: 
      labels: 
        app: {{ template "gitlab.fullname" . }}
    spec: 
      hostname: gitlab-test
      containers: 
      - name: {{ template "gitlab.fullname" . }}
        image: gitlab/gitlab-ce:11.4.7-ce.0
        ports: 
          - containerPort: 80
          - containerPort: 22
          - containerPort: 443
        volumeMounts: 
        - mountPath: "/etc/gitlab"
          name: {{ .Values.appconfig.pvc }}
        - mountPath: "/var/log/gitlab"
          name: {{ .Values.applog.pvc }}
        - mountPath: "/var/opt/gitlab"
          name: {{ .Values.appmain.pvc }}
      volumes: 
        - name: {{ .Values.appconfig.pvc }}
          persistentVolumeClaim:
            claimName: {{ .Values.appconfig.pvc }}
        - name: {{ .Values.applog.pvc }}
          persistentVolumeClaim:
            claimName: {{ .Values.applog.pvc }}
        - name: {{ .Values.appmain.pvc }}
          persistentVolumeClaim:
            claimName: {{ .Values.appmain.pvc }}
