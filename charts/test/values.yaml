image:
  repository: reg.cecii.cn
  tag: v3.0
  port: w
  pullPolicy: Always
    
service:
  type: ClusterIP

ingress:
  enabled: true
  hosts:
    - host: elfinder.os.cecii.cn
  tls: []

nameOverride: ""
fullnameOverride: ""

replicaCount: 1

volumes: 
  - mountPath: w
    size: 1Gi
    cephPath: ${cephpath}/default

