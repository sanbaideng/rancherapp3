apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "gitlab.fullname" . }}
spec:
  selector:
    matchLabels:
      app: {{ template "gitlab.fullname" . }}
  strategy: 
    type: Recreate
  template: 
    metadata: 
      labels: 
        app: {{ template "gitlab.fullname" . }}
    spec: 
      hostname: gitlab-test
      containers: 
      - name: {{ template "gitlab.fullname" . }}
        image: gitlab/gitlab-ce
        ports: 
          - containerPort: 80
          - containerPort: 22
          - containerPort: 443
        volumeMounts: 
        - mountPath: "/etc/gitlab"
          name: {{ .Values.appconfig }}
        - mountPath: "/var/log/gitlab"
          name: {{ .Values.applog }}
        - mountPath: "/var/opt/gitlab"
          name: {{ .Values.appmain }}
      volumes: 
        - name: {{ .Values.appconfig }}
          persistentVolumeClaim:
            claimName: {{ .Values.appconfig }}
        - name: {{ .Values.applog }}
          persistentVolumeClaim:
            claimName: {{ .Values.applog }}
        - name: {{ .Values.appmain }}
          persistentVolumeClaim:
            claimName: {{ .Values.appmain }}
