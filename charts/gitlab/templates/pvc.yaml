apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.appmain.pvc | quote }}
  namespace: {{ .Values.global.namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: {{ .Values.appmain.capacity }}
  selector:
    matchLabels:
      name: {{ .Values.appmain.pv | quote }}
