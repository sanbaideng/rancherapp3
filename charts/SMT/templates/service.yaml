apiVersion: v1
kind: Service
metadata:
  name: {{ template "smtall.fullname" . }}
  labels:
    app: {{ template "smtall.name" . }}
    chart: {{ template "smtall.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ template "smtall.name" . }}
    release: {{ .Release.Name }}
