apiVersion: v1
kind: Service
metadata:
  name: {{ template "steelmeshlist.fullname" . }}
  labels:
    app: {{ template "steelmeshlist.name" . }}
    chart: {{ template "steelmeshlist.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ template "steelmeshlist.name" . }}
    release: {{ .Release.Name }}
