apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.applog.pvc | quote }}
  namespace: {{ .Values.global.namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: {{ .Values.applog.capacity }}
  selector:
    matchLabels:
      name: {{ .Values.applog.pv | quote }}
