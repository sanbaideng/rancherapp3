apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.appmain.pv | quote }}
  labels:
    name: {{ .Values.appmain.pv | quote }}
    namespace: {{ .Values.global.namespace | quote }}
spec:
  capacity:
    storage: {{ .Values.appmain.capacity }}
  accessModes:
    - ReadWriteOnce
  cephfs:
    monitors:
      - {{ .Values.global.storageAddr }}
    path: {{ .Values.appmain.path }}
    user: {{ .Values.global.cephUser }}
    secretRef:
      name: {{ .Values.global.cephSecret }}
      namespace: default
    readOnly: false
  persistentVolumeReclaimPolicy: Retain
